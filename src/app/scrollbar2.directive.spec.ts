import { Scrollbar2Directive } from './scrollbar2.directive';

describe('Scrollbar2Directive', () => {
  it('should create an instance', () => {
    const directive = new Scrollbar2Directive();
    expect(directive).toBeTruthy();
  });
});
