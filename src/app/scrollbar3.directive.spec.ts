import { Scrollbar3Directive } from './scrollbar3.directive';

describe('Scrollbar3Directive', () => {
  it('should create an instance', () => {
    const directive = new Scrollbar3Directive();
    expect(directive).toBeTruthy();
  });
});
